syntax = "proto3";

package clipboardsync;

service ClipboardSync {
    rpc Sync (Clipboard) returns (Copied) {}
}

message Clipboard {
    string data = 1;
}

message Copied {
    bool success = 1;
}